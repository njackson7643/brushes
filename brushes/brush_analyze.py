import os
import sys
import math

#analyzes the lammps.trj file and compute the mean brush height

#analyze the lammps.trj file and computes the number density of polymers, salt, and cations as a function of z position
